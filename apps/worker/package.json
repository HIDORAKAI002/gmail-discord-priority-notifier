{
  "name": "@mailsync/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/apps/worker/src/index.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mailsync/shared": "0.1.0",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
